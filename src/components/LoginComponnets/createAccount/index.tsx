import React from "react";
import { Container, TextComponent, TextLink } from "./styles";

const CreateAccount = () => {
  return (
    <Container>
      <TextComponent>Don't have and account?</TextComponent>
      <TextLink>Sign up.</TextLink>
    </Container>
  );
};

export default CreateAccount;
